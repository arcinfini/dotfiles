
# Start

Like most other neovim setups, a Nerd Font is required to be installed to receive the most from the plugins included in this pack.


On first usage of Neovim after loading this into the user directory, Lazy will download and install the configuration.
